rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Scanned receipt images, uploaded by SpendingTrackerScreen after OCR.
    match /receipts/{userId}/{fileName} {
      allow read, write: if request.auth != null && request.auth.token.email == userId;
    }
  }
}
